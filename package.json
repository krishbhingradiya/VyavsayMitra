{
  "name": "vyavsaymitra",
  "private": true,
  "version": "1.0.0",
  "description": "Rural Business Feasibility & Advisory Intelligence Platform",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix backend test",
    "test:backend": "npm --prefix backend test",
    "test:ml": ".venv\\Scripts\\python.exe -m unittest discover -s tests -p \"*_test.py\"",
    "test:all": "npm run test:backend && npm run test:ml",
    "lint": "npm --prefix frontend run lint",
    "install:all": "npm install && npm --prefix frontend install && npm --prefix backend install"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
